name: authentication_repository
description: Dart package which manages the authentication domain.
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  equatable: ^2.0.3
  uuid: ^3.0.0
  http: ^0.13.4
  dartz: ^0.10.0
